import stylex from "@ladifire-opensource/stylex";

export const styles = stylex.create({
  flexColumn: {
    display: 'flex', 
    flexDirection: 'column'
  },
  tremeloContainer: {
    display: 'flex', 
    justifyContent: 'center', 
    margin: '10px auto auto'
  },
  pitchContainer: {display: 'flex', margin: 'auto'}
});